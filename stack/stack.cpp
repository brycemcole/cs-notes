#include <iostream>
#include <stack>

int main() {
    std::stack<int> s;

    s.push(10);
    s.push(4);
    s.push(9);

    std::cout << s.empty() << "\n";

    s.pop();
    s.push(4);

    std::cout << s.size() << "\n";

    s.swap

    return 0;
}